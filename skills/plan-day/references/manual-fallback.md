# Manual fallback for plan-day

No task-manager connector was detected. Ask the user to list today's fixed commitments and candidate next actions directly, build the plan conversationally using the four-fold model, and present it as text. Note that connecting a task manager would let this plugin pull scheduled tasks and apply completions/reschedules automatically.
